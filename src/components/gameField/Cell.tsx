import React from 'react';
import style from '../styles/cell.module.css'

const Cell = () => {

    return (
        <div className={style.wrapper}>
            
        </div>
    );
};

export default Cell;